function init() {
    showBookLibrary();
    
}
function showBookLibrary() {
    let libraryContainer = document.getElementById("library_container");
    libraryContainer.innerHTML =""; //Inhalt leeren
    for (let indexBook = 0; indexBook < book.length; indexBook++) {
        const book = book[indexBook];
        // Ein Buch entspricht ein Array aus der liste.
        
    }
    
}